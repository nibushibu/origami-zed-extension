/target
node_modules/
.DS_Store

# Build artifact produced by Zed when compiling the extension
/extension.wasm

# Zed clones the Tree-sitter grammar repo into here at install time; the
# grammar source lives in its own repository (see [grammars.origami] in
# extension.toml), not in this repo.
/grammars/
